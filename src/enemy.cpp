#include "enemy.h"

Enemy::Enemy(){};
Enemy::~Enemy(){};

void Enemy::update(RenderWindow& window, std::vector<std::unique_ptr<Enemy> >& enemies, Player& player, std::vector<std::unique_ptr<Explosion> >& explosions, bool& gameOver) {
    
}

void Enemy::move(){};

void Enemy::reset(){};
